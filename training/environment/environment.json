{
  "schema_version": "1.1",
  "purpose": "Svetlana Gemma 4 E2B production text SFT and baseline evaluation",
  "provider": "Google Colab",
  "target_gpu": "Tesla T4 15 GB or better",
  "python": "3.13",
  "cuda_wheel": "cu126",
  "model": "google/gemma-4-E2B-it",
  "requirements": "training/environment/requirements-colab-gpu.txt",
  "status": "fresh_runtime_validation_pending",
  "validation_required": [
    "pip install succeeds in a fresh Colab runtime",
    "exact pinned package versions match the repository requirements",
    "torch.cuda.is_available() is true",
    "all package imports succeed",
    "model and processor/tokenizer load",
    "baseline inference completes",
    "production SFT reaches the configured final optimizer step",
    "adapter and training evidence are written",
    "adapter inference completes",
    "adapter evaluation is compared against baseline"
  ]
}
